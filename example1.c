#include <stdio.h>
/* Tis is a comment */

int main () {
	printf("Hello World!\n");
	return 0;
}
/* Use the -save-temps flag with gcc to see all “source code -> preprocessor -> compiler -> assembler -> linker -> binary executable” files. */
